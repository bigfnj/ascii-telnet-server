{
  "schema": 1,
  "path": ".github/workflows/tests.yml",
  "sha1": "485fe499ba128319c8c284eccdb22fa71ea72934",
  "purpose": "GitHub Actions CI workflow replacing Travis. Runs the pytest suite on Ubuntu across Python 3.10 through 3.13 after editable package installation.",
  "exports": [
    "tests workflow"
  ],
  "imports": [
    "actions/checkout@v4",
    "actions/setup-python@v5",
    "pyproject.toml"
  ],
  "called_by": [],
  "calls_out_to": [
    "python -m pip install -e .[dev]",
    "python -m pytest"
  ],
  "invariants": [
    "The Python version matrix should match pyproject.toml requires-python classifiers.",
    "CI installs the package editable before running tests so console-script and packaging metadata stay exercised."
  ],
  "gotchas": [
    "The workflow assumes GitHub Actions; .travis.yml has been removed."
  ]
}
